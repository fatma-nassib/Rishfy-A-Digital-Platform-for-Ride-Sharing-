import type { ReactNode } from "react";

// Minimal stub Toaster component to satisfy imports.
// You can replace this with the full shadcn/ui toaster implementation later.
export function Toaster(_props: { children?: ReactNode }) {
  return null;
}

