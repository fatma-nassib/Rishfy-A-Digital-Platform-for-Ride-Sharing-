import type { ReactNode } from "react";

// Very small TooltipProvider stub used at the app root.
// Replace with the full shadcn/ui tooltip primitives if needed.
export function TooltipProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

